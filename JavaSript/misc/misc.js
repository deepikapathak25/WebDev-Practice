const sum = (a,b)=> {
    console.log(a+b);
};

const cube=(n)=>{
    return n*n*n;
};

const pow=(a,b)=>{
    return a**b;
};

//implicit = pahle se understood hai ki value return krni hai
//explicit = alag re btana hai ki value return krni hai

const mul = (a,b)=>(a*b);